import 'package:flutter/material.dart';

class HomeWallScreen extends StatelessWidget {
  const HomeWallScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("HomeWallScreen"),
    );
  }
}
